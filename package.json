{
  "name": "BytesTouchLog",
  "version": "1.0.0",
  "description": "CAPSTONE 2",
  "scripts": {
    "bytes": "cd bytes && npm run serve",
    "server": "cd server && npm start",
    "all": "npm run bytes && server",
    "test": "cd server && cd src && cd test && node testhash.js",
    "test:frontend": "cd bytes && npm run test:unit",
    "test:backend": "jest --config server/jest.config.js",
    "test:all": "npm run test:frontend && npm run test:backend"
  },
  "dependencies": {
    "bcrypt-nodejs": "^0.0.3"
  },
  "devDependencies": {
    "@vue/test-utils": "^2.0.0-rc.18",
    "babel-jest": "^29.0.0",
    "jest": "^29.0.0",
    "vue-jest": "^29.0.0",
    "babel-core": "^7.0.0-bridge.0",
    "babel-preset-env": "^1.7.0"
  }
}
